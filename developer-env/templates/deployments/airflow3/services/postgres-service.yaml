apiVersion: v1
kind: Service
metadata:
  name: airflow3-postgres
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
  - name: airflow-postgres
    port: 5432
    targetPort: 5432
  selector:
    app: airflow3-postgres
